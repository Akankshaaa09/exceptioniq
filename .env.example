# Local PostgreSQL connection (keep secrets out of Git)
PGHOST=localhost
PGPORT=5432
PGDATABASE=exceptioniq
PGUSER=postgres
PGPASSWORD=your_password_here

# For Streamlit Cloud, prefer a hosted PostgreSQL connection string:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
